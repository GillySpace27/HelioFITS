# Build output
build/
DerivedData/
*.xcarchive
# macOS
.DS_Store
# Xcode user state
xcuserdata/
*.xcuserstate
# Importer build artifacts
FITSMetadataImporter/build/
# Ship output
ship.log
# stray object files (fitsshim.c is vendored; .a provides it)
*.o
__.SYMDEF*
.venv/

# App Store Connect API keys must never live in the repo
*.p8
